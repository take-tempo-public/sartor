---
status: review-artifact
evidence_sha: c6e0437
graduation: none
---

# Verification log — 2026-06 product-excellence review

> Adversarial verification of every **P0/P1** finding in the eight-domain
> assessment of **callback.** For each: the verifier re-derived all cited
> evidence at `c6e0437`, attempted falsification, recorded
> counter-evidence, and (where the attempt landed) a revised claim. Honors
> C-0 claims discipline — checks were static git/grep/AST or sandboxed
> dynamic probes with no writes inside the repo and no paid/LLM calls
> unless noted.
>
> Verdicts: **CONFIRMED** (substance and severity stand), **WEAKENED**
> (substance survives, a superlative/severity/sub-claim trimmed — read the
> revised claim), **REFUTED** (none this review). P2/P3 findings are in the
> [register](findings-register.md) but were not adversarially re-verified.
>
> Tally: 42 P0/P1 findings — 36 CONFIRMED, 6 WEAKENED, 0 REFUTED. Both P0s
> CONFIRMED.

---

## Product vision & definition

### F-vision-01 — flat constraint tier vs charter enforceability tiering — **WEAKENED**
- **Tried:** could not break the substantive claim — the flattening is real and the charter demonstrably wants the tiering. Tested the "single largest vision-doc divergence" superlative (sourced verbatim from the domain guide, not invented; framed on a different axis than F-vision-02 so the two superlatives coexist) and the sub-section count.
- **Counter-evidence:** none beyond two wording imprecisions; disposition and severity stand.
- **Revised:** keep FIX/P1 and full substance. (1) Cite **six** sub-sections (vision.md:81-174 has six `###` headers), not "~7". (2) **Drop or footnote the "C-8" trace** — there is no charter clause C-8; the anchors are C-0 plus the charter's C-1..C-6 / D-1..D-6 two-tier structure. "C8" is discovery-brief item #8, not a clause ID.

### F-vision-02 — absolute no-invention register violates C-0 — **CONFIRMED**
- **Tried:** re-derived all cited lines at c6e0437; four refutation angles — (1) C-0 scope escape (both docs are repo-public, A-4-addressed; no escape); (2) already-mechanism-qualified (lead sentences are flat impossibility/categorical claims); (3) "may not fabricate" softness (system-model.md:31/33 remain categorical with no by-construction enforcement); (4) already-remediated (none in-flight). Read the interview record to confirm the owner recanted the exact strings.
- **Counter-evidence:** none overturns it. C-0 is signed and bars LLM-behavior absolutes; R2-4.2/R2-4.4 quote the owner striking the exact phrases; vision.md:50 still reads "The LLM cannot invent facts" and :151 still heads "No invention, ever." Minor non-weakening caveat: system-model.md:162's revision point is framed as narrative-ordering, but the finding's wording does not overclaim. P1/FIX well-supported (highest-audience docs, A-4 reader, S-1 amateurish-miss class).

### F-vision-03 — interviews not a success criterion; outcome loop shipped but docs say "(Future v2)" — **CONFIRMED**
- **Tried:** traced the outcome chain end-to-end — migration 0006 adds sent_at/outcome_at + expands the status CHECK; CheckConstraint pins status incl. interview; PUT /api/applications/<id>/status auto-stamps; app.js renders the status-funnel buttons; tests exercise the route. So shipped UI->route->DB, not dead schema. Searched PRODUCT_SHAPE/vision/README for any reconciliation.
- **Counter-evidence:** none undermining substance. The "Mark sent + outcome" capability is shipped yet PRODUCT_SHAPE still frames it as not-shipped in SIX places, none reconciled; vision never elevates interviews to a stated success criterion. One real citation defect: the trace line "C-6 (success-loop)" is wrong — C-6 is the deterministic-LLM boundary.
- **Revised:** holds at FIX/P1. Correct the trace to **P-4, M-1, T-A** (C-2 sets the no-aggregate-observation constraint the fix must scope to); the Fix text itself already cites C-2/T-A correctly. Substance/evidence/severity unchanged.

### F-vision-04 — single-tenant-as-value contradicts "local and yours" — **CONFIRMED**
- **Tried:** re-derived cited lines; five break angles — (1) does R2-2 ("one person... survives") contradict the rejection? (2) is "leads with the rejected sentence" overstated? (3) does the multi-profile counter-evidence hold? (4) is "single-tenant" merely the threat model (doc technically correct)? (5) is P1/FIX + C-1/P-2/A-2 justified? Read the full R2-4.1 resolution.
- **Counter-evidence:** none refutes. R2-2's "survives" is superseded by the later R2-4.1, where the owner reconsidered the exact sentence and flagged the "single-tenant by design" claim as overstated -> charter+prescription. list_users()/create_user/userSelect = genuine multi-profile. The proposed fix preserves the single-unauthenticated-user threat model, demoting only the value/scope claim. Minor non-fatal: "leads with" is loose; A-2 is a weaker tie than P-2/C-1. Core claim and FIX/P1 stand.

### F-vision-05 — "no third-party CDN fetches at runtime" false at the pin — **CONFIRMED**
- **Tried:** six angles — (1) pin integrity (`git diff c6e0437 HEAD` empty on the cited files); (2) both evidence anchors verbatim; (3) "not at runtime" (route is live, registered, tested; script tag unconditional, no Jinja guard — emitted every render); (4) PX-01 already landed (no vendored chart*.js found); (5) "ahead of SECURITY.md" side-claim; (6) severity/trace vs C-0/S-1.
- **Counter-evidence:** core holds — vision.md:92 asserts a categorical no-CDN-egress claim contradicted by the unconditional cdn.jsdelivr.net Chart.js fetch on every /_dashboard render; PX-01 not landed. Two minor overstatements, neither breaking: (a) post-vendor the sentence becomes a TRUE permitted categorical (C-0 permits categoricals in deterministically-enforceable domains incl. network egress) — the live violation is the present falsity, not the form; (b) SECURITY.md:85 carries its own parallel false categorical (out of this finding's vision scope; flagged in F-sec-03).

---

## Architecture & code health

### F-arch-01 — C-6 boundary convention-only; no import-lint/boundary test — **CONFIRMED**
- **Tried:** hunted every construction-level gate — grepped pyproject + tree for import-linter/tach/grimp (none); read ci.yml (ruff+mypy+pytest only); searched tests/ for AST/importlib boundary inspection (only importlib.reload for isolation); read conftest (no session guard); inspected all 10 hooks (none touch the LLM boundary); cross-checked the 7 deterministic modules genuinely stay clean.
- **Counter-evidence:** none — no mechanism anywhere fails on a regression. Behavior holds by convention, not by a gate; C-0 names module boundary as a construction-enforcement target, so C-6's "Inviolable" is a categorical lacking the backing C-0 prescribes.
- **Revised:** substance fully confirmed. One severity caveat: P1 sits slightly high against S-1 ordering (the boundary currently holds, so this is hardening/claims-discipline, not an active defect or PII risk). Consider P1->P2 as a hardening item; the gap is real and correctly traced.

### F-arch-02 — v1.0.8 blueprint plan carries stale blast-radius numbers — **CONFIRMED**
- **Tried:** re-derived all four metrics on the c6e0437 snapshot via `git archive`. Tried LOC interpretations to rescue 6290 (total 6992 / non-blank 6179 / non-blank-non-comment 5713 — none match); route interpretations (78 decorators / 64 unique paths — neither is 75); a transitive/conftest reading to rescue 67 (conftest does not import app); confirmed import-leaf (0 prod modules import app); checked drift history.
- **Counter-evidence:** none — app.py=6992 (vs 6290), 78 @app.route (vs 75), 24 test files import app (vs 67), 0 prod modules import app. The "67 test files... MUST NOT interleave" rationale is the load-bearing argument for the v1.0.8 epic and overstates real coupling by ~2.8x.
- **Revised:** P1 stands as a **docs-accuracy** P1 (stale planning figures in the internal RELEASE_ARC), not behavioral/security. Nuance: LOC and routes drifted UP, test-coupling is OVERSTATED — all three wrong; actuals 6992/78/24 each correct.

### F-arch-03 — route-security-lint hook app.py-scoped, dark on blueprints — **WEAKENED**
- **Tried:** confirmed hard app.py scope (`(^|/)app\.py$` + `@app\.route\(` literal — cannot scan blueprint decorators); found exactly ONE blueprint at the pin (dashboard_bp, one route); tried to show a LIVE hole (route body uses only hardcoded paths; user input is in-memory equality filters, never path construction; before_request localhost guard; read-only); checked whether the project already knows (RELEASE_ARC:753-754 + excellence-walk.md:214 log it as a planned-mitigation item); checked docs for a C-0 over-claim.
- **Counter-evidence:** structural claim holds (hook is app.py-only; dashboard_bp uncovered) but severity P1 and the "already uncovered live gap" implication are overstated: the single blueprint route is localhost-gated, read-only, constructs no path from user input — missing guards would guard nothing; the gap is already known/documented; the C-0 angle is largely unsupported (6+ docs accurately scope the hook to app.py; only SECURITY.md:211 is mildly overbroad).
- **Revised:** keep the finding, downgrade FIX/P1 -> **P2/P3** (latent guardrail-coverage gap + one-line SECURITY.md:211 tightening, not a present security hole). The coverage gap becomes load-bearing during the planned blueprint refactor when filesystem-touching routes migrate. Two actions: (i) extend the hook's matcher beyond app.py+@app.route when blueprints land (already a RELEASE_ARC:753-754 task); (ii) scope SECURITY.md:211 to app.py-resident routes.

---

## Product experience & accessibility

### F-expa11y-01 — a11y taxonomy NOT machine-checked in CI (local-only) — **CONFIRMED**
- **Tried:** 8 fronts — enumerated every workflow (ci.yml only; quality job has no `playwright install`/`-m ux`); checked `pip install -e .[dev]` does not provision the browser binary; reasoned the fresh-runner launch() raises -> pytest.skip; verified the skip mechanism (conftest `_browser` skips); grepped for any ux test launching its own browser (none); checked D-6 does not sanction the CI gap; verified the E-2 taxonomy phrasing; confirmed Chromium IS present on the maintainer machine (matching the finding's own framing).
- **Counter-evidence:** none weakens it. A maintainer-local pre-merge gate exists, but the finding already scopes the gap to UNATTENDED CI. The RELEASE_ARC:18 "ux green" overstatement is owned by the sibling quality-engineering finding. E-2's "in CI, free forever" anchors P1.

### F-expa11y-02 — cold no-API-key user error-dumped mid-analyze — **CONFIRMED**
- **Tried:** sandboxed SDK checks (no paid calls) — empty key builds a client with no construction raise; AuthenticationError is NOT an APIConnectionError; grepped repo for AuthenticationError/error-handler (zero); confirmed empty key yields a completed 401 round-trip; traced both analyze paths (only APIConnectionError + LLMResponseError caught); confirmed onboarding is a separate CLI path.
- **Counter-evidence:** core holds. One symptom imprecision: the NON-stream path does NOT return the cited 503 — an AuthenticationError is uncaught there and surfaces as a bare Flask 500/traceback (debug default), even less guided. The streaming-path attribution (generic 500 "Internal error during analysis.") is exact.
- **Revised:** stands at FIX/P1. Correct the symptom: non-stream path -> bare 500/traceback (not the cited 503); only the streaming 500 is as stated. Substance and severity unchanged or slightly worse. Cheap fix: detect empty/missing key before any LLM call, and/or add an `except anthropic.AuthenticationError` arm.

### F-expa11y-03 — no ACCESSIBILITY.md honest-status page at the pin — **CONFIRMED**
- **Tried:** re-ran `git ls-tree -r c6e0437` (no ACCESSIBILITY.md; only tests/ux/a11y/*); repo-wide grep for the token (zero); verified the E-2 charter quote verbatim; checked whether the deliverable is scheduled in RELEASE_ARC v1.0.7 (appears nowhere in the arc); checked walkthrough-sprint-plan screen-reader hits; searched README/SECURITY/docs for a substitute (none).
- **Counter-evidence:** none — absence is real and the charter quote accurate. The only NVDA reference (CHANGELOG:1702) states the manual pass is pending+undocumented (reinforces). The pin sits mid-v1.0.6 so the file legitimately does not exist yet; the finding flags the absence of any home or scheduled slot, which is accurate.

### F-expa11y-04 — diagnostics legends dev-register; lay metrics legend unwritten — **CONFIRMED**
- **Tried:** re-derived every cited line; hunted counter-evidence — (1) grepped the dashboard for a glossary/"what this score means" (found two partial-lay legends at :668/:693 the finding did NOT cite — could break "Every legend reads as internals"); (2) confirmed axe scans /_dashboard; (3) read the tuning detail pane (dense dev-register); (4) checked RELEASE_ARC (the lay-legend sweep is scheduled Sprint 6.5); (5) cross-checked S-3/M-2/E-2/A-2.
- **Counter-evidence:** factual core holds — the always-visible legends/banners are dev-register and the lay metrics legend is unbuilt and scheduled (Sprint 6.5 / M-2 v1.0.7). Two qualifications, neither refuting: (a) "Every legend" is slightly overstated — :668/:693 carry partial lay glosses but remain jargon-laden and gated behind a tile click; (b) this is a tracked, planned deferral, which the finding already discloses. P1/FIX defensible (S-3 = owner's self-named weakest area; M-2 makes the legend an explicit criterion).

### F-expa11y-07 — keyboard bullet-reorder alternative pinned by a real test — **CONFIRMED**
- **Tried:** re-derived via `git show` (not the worktree) — buttons are real `<button>` with aria-labels wired to _moveBulletRow; traced persistence to break "byte-identical" (both keyboard + drag call the SAME _markCustomOrder + debounced save built from DOM order); checked the test doesn't fake the route (resolves by accessible name; asserts persistence across navigate-away+reload re-fetching GET /composition); verified the server round-trips bullet_order; verified buttons render in compose; tested the silent-rot caveat (CI runs bare pytest, no chromium install -> collected then skipped).
- **Counter-evidence:** none — all evidence re-derives. Minor non-material nuances (drag-path band starts a few lines early; label compression). "Fully resolved and guarded" is not overstated — the finding attaches the explicit CI-skip caveat (guarded = a real pinning test exists, not = runs in CI).

### F-expa11y-08 — _announce() live-region discipline intact at every async completion — **CONFIRMED**
- **Tried:** enumerated every _announce/srAnnounce reference (8 in app.js: 7 calls + definition; one region in index.html; no second writer); read each of the 7 call sites (all on real success paths, errors diverted to reportError/alert); verified sr-only clip + off-then-on re-announce; checked whether other aria-live regions over-scope the claim (the claim is scoped to the _announce region, which is singular); probed the KEEP-protect rationale (no test asserts the announcements; axe cannot catch a removed call/renamed id).
- **Counter-evidence:** two narrow caveats, neither refutes. (a) The helper comment lists "errors" but errors actually route through reportError() — a doc-comment imprecision, not a finding defect (the finding lists only the 7 non-error completions). (b) The "breaks silently" rationale is verified TRUE (no test guards them) — strengthens the finding. Region attributes all match.

### F-expa11y-09 — modal focus-trap + Escape + focus-return; vendored axe gate — **WEAKENED**
- **Tried:** re-derived every cited line (worktree HEAD b85be08, diff to c6e0437 empty); checked both traps implement Tab-wrap + Escape + focus-return; whether the edit modal's initial focus sits outside the trap set; whether axe can silently skip; whether the test scans every dashboard tab; whether any test exercises the modals; the MPL-2.0 header; E-2 "no traps" vs an intentional escapable modal.
- **Counter-evidence:** core mechanisms hold by static reading (both traps wrap Tab, Escape->cleanup, focus-return; axe MPL-2.0 v4.10.2, no pip dep, cannot silently skip; gate scans all 5 /_dashboard tabs). The ONE weakness: grep of tests/ for both modals + triggers returned NOTHING — neither modal's behavior is exercised by any test; the source comment says "Phase 4 will verify." The cited axe test loads /_dashboard but does NOT open either modal, so the two evidence clusters are independent, not mutually reinforcing.
- **Revised:** keep, but narrow — both modals **implement** (by static reading) an escapable trap consistent with E-2, but the behavior is **NOT yet test-verified**; read "in working form" as "implemented per static inspection, behavior not yet covered by the suite." The vendored-axe claims are fully accurate and independently solid. Note the cited axe test does not exercise the modals.

---

## Quality engineering & release discipline

### F-qe-rel-01 — UX/a11y/PDF tier silently skips in CI (P0) — **CONFIRMED**
- **Tried:** proved the cited files are byte-identical at c6e0437; ci.yml is the sole workflow (no Scorecard/UX job; no `playwright install`/chromium in either job); confirmed default pytest COLLECTS the axe tests (addopts has no marker filter) -> collected-then-skipped; DYNAMIC sandbox check (temp venv, playwright wheel without browser binary -> chromium.launch() raises -> conftest pytest.skip; pdf skipif False) reproduces the silent skip; confirmed E-2 "in CI, free forever" is owner-agreed (round2-verification-brief:52) and signed; checked severity P0.
- **Counter-evidence:** none breaks it. test_pdf_render.py:13 "CI is configured to run it" and RELEASE_ARC's v1.0.5 "ux green" both overstate CI reality at the pin (the ux-green was maintainer-local), exactly as the finding states. Nuance: the non-Chromium PDF class + LLM-stubbed unit suite still run, but the finding scopes its claim to the browser-dependent paths. P0 defensible (owner-agreed signed E-2, unguarded ATS/PDF render path, A-2 surfaces, S-1 glaring-miss class).

### F-qe-rel-02 — C-2 no-egress asserted, not machine-falsifiable (P0) — **CONFIRMED**
- **Tried:** `git ls-tree` + tree-wide greps for egress/allowlist/socket/pytest-socket/disable_socket/falsifiab (no enforcing test/conftest); read test_scraper (only stubs scraper.requests.get, no destination assertion); read test_app_security (filesystem-only); read conftest (no socket disabling); enumerated the outbound surface (Anthropic SDK default base_url + scraper user-URL fetch + sanctioned HF download) -> enumerable, so a test is feasible; checked scraper has no internal host allowlist; checked severity is a BOOST not a false-claim defect.
- **Counter-evidence:** none — every fact re-derived true. C-2's machine-verifiability was a one-time code/doc audit, not a committed gate; C-0 confines categoricals to those a deterministic test enforces by construction (C-2's "leave the machine, ever" has none); E-2 lists a "network-egress falsifiability test" as a desired badge. The only nuance (C-2 is not factually wrong, just unenforced) is exactly how the finding frames itself.

### F-qe-rel-03 — none of the agreed E-2 machine badges exist at the pin — **CONFIRMED**
- **Tried:** `git ls-tree -r c6e0437` over the whole tree + .github (no dependabot/Scorecard/LICENSES/REUSE.toml/lockfile); grepped ci.yml for scorecard/reuse/dependabot/egress (none); grepped for an egress falsifiability test (none); grepped SPDX-License-Identifier (absent); confirmed deps are floating ranges, LICENSE MIT-only, axe MPL-2.0; hunted for a NOTICE/doc-level axe declaration; checked SECURITY.md for PVR; checked D-1/CONTRIBUTING for lockfile language; confirmed E-2 'agreed' is owner-voiced (R2-12); confirmed the pin..HEAD commits are review-artifact-only.
- **Counter-evidence:** core holds — all five E-2 items verifiably absent. Three minor counter-points, all cutting toward LOWER severity (finding already at P1): (a) tests/ux/a11y/vendor/README.md DOES declare axe MPL-2.0 in prose and paged.polyfill.js as MIT — only the REUSE machine-readable form is absent; (b) SECURITY.md:129 routes vuln reports to an advisory but that is not the PVR badge; (c) the D-1 link is loose (D-1 mandates no lockfile/reproducibility).
- **Revised:** optional tightening (does not change CONFIRMED/P1): note axe's MPL-2.0 is already declared in human-readable prose, so REUSE adds machine-readable/aggregated declaration, not first-time honesty; trace the lockfile/Dependabot benefit primarily to E-2 (its exact wording) rather than D-1.

### F-qe-rel-04 — C-6 boundary holds by convention; no gate fails on an LLM import — **CONFIRMED**
- **Tried:** AST-parsed all 7 modules from c6e0437 (Import/ImportFrom incl. relative levels) -> zero analyzer/anthropic imports; hunted any missed enforcer — grepped tests for AST/importlib/sys.modules/inspect (only reload for isolation), import-absence assertions (none), conftest (no guard); checked pyproject for import-linter/grimp + a ruff TID/banned-api rule (ruff select = E,W,F,I,B,UP,S — no TID); read all 10 hooks (none enforces the boundary); read ci.yml (ruff/mypy/pytest only); tested the C-2-vs-C-6 contrast; checked severity inflation (finding concedes the boundary holds).
- **Counter-evidence:** none — every candidate enforcer location came up empty. The "no LLM"/"LLM-free" hits are docstrings/comments (convention), not executable assertions. C-0 names "module boundary" as a should-be-construction-enforced category; C-6 calls the boundary "Inviolable." P1/FIX proportionate; the cited fix (~15-line AST test or import-linter contract) is exactly the construction C-0 prescribes.

### F-qe-rel-05 — eval-quality regression gate exists and blocks; affirm — **CONFIRMED**
- **Tried:** re-derived the full mechanism statically + dynamically — REGRESSION_DELTA=0.5; _detect_regression sets is_regression on delta<-0.5; regressions populate exit_code=2 (independent of n_fail); main() returns it; sys.exit propagates; ci.yml eval-smoke is label-gated with no continue-on-error so exit 2 fails the step. Imported runner (no paid calls) and called _detect_regression (4.4->3.8 = True). Checked what's committed in evals/results/ (only baseline_v1.json) so a clean CI seeds from the baseline means.
- **Counter-evidence:** mechanism holds end-to-end. One scope refinement: CI runs `--subset smoke`, and _select_rubrics returns ONLY the grounding rubric for smoke -> the live CI gate covers 3 (fixture x grounding) pairs, NOT the full matrix (which only fires on the manual `--suite synthetic` path ci.yml does not invoke). Two minor nuances: comparison is seeded-from not pinned-to baseline_v1.json (latent); the 0.5 boundary is float-representation-dependent (~0.5).
- **Revised:** keep KEEP/P1. Tighten one phrase: in CI the gate is **grounding-rubric-only across the 3 synthetic fixtures**, not the full matrix. Add to the existing caveats (label-gated, synthetic-only). None of this alters the P1 KEEP rationale: a real machine-run gate that forces exit 2 and fails eval-smoke on a grounding-score drop near/past 0.5 vs the committed baseline mean.

### F-qe-rel-07 — T-D not closed; eval/grounding gates run only on synthetic fixtures — **WEAKENED**
- **Tried:** re-derived all evidence; three fronts — (1) any standing gate on real data (only label-gated eval-smoke on synthetic; fixtures/real = .gitkeep so `--suite real` is non-runnable; baseline = 3 synthetic); (2) whether deterministic gates catch the named examples (test_parser/test_pdf_render run in the un-gated pytest job but on small constructed inputs, not robert-scale); (3) whether T-D is already a named release-blocking task (found RELEASE_ARC:712 PV-1, :325 permanent `--suite real` set, PV-2/PV-3 into v1.0.7 tag criteria, RELEASE_CHECKLIST:47-49, V1_0_5_VERIFICATION, PRODUCT_SHAPE:590).
- **Counter-evidence:** factual core holds (synthetic-only standing gates; baseline = 3 synthetic; real dir empty; T-D/M-2 accept it) — CONFIRMED part. The overstatement: the recommendation ("track as a named release-blocking task, not a silent assumption") is partly redundant — at the pin it IS already a named, sequenced, release-blocking task set. Named examples partly mitigated (parser + PDF render ARE covered by un-gated unit tests, on small inputs). Minor citation error: PERFORMANCE_HISTORY.md is at docs/dev/perf/, not root.
- **Revised:** keep WATCH/P1 for the unclosed-at-pin gap (standing gates run on synthetic/small-constructed inputs; real-corpus-and-scale-dependent regressions are not caught; baseline + CI eval-smoke synthetic-only). **Drop the "silent assumption" framing** — at c6e0437 this is already named/sequenced (PV-1/PV-2/PV-3 -> v1.0.7 + permanent `--suite real` set; M-2's 10-real-applications gate). The finding's value is the reminder that M-2 evidence is not yet produced. Fix the cite to `docs/dev/perf/PERFORMANCE_HISTORY.md:93-97`.

### F-qe-rel-08 — two hard human SLAs ship, violating D-4 — **CONFIRMED**
- **Tried:** re-derived both quotes verbatim at the pin; confirmed D-4 is a signed clause (sig line 360) that bans response-time SLAs and explicitly names "SECURITY.md's 5-business-day vulnerability response"; checked already-fixed (both lines still unsoftened at HEAD); tested the "hard SLA" characterization (SECURITY.md "aim to respond" partial hedge + hard "fix within 30 days"; COC "will respond"); checked the coordinate vs the public tag.
- **Counter-evidence:** none — evidence re-derived exactly; D-4 signed and names the exact promise; both lines unsoftened at HEAD; finding scopes to "before the public tag," not overstated. One non-weakening nuance: SECURITY.md:134's "We aim to respond" is partially hedged, but D-4 bans day-count SLAs categorically, so the hedge plus the harder 30-day-fix and COC "will respond" still violate.

---

## Eval / grounding / tuning as product

### F-eval-01 — AL-1 over-suppression uninstrumented and unfalsifiable from data — **CONFIRMED**
- **Tried:** re-derived via `git show`; located the always-on ride-along set (_post_generation_metrics — no bullet count); hunted candidate counter-metrics (bullet_count, selected_bullets); traced the eval pipeline (analyze->clarify->generate only, never recommend/_dropoffPick); confirmed the charting surface; `git log -L`/`-S` to test whether the two count-shaping mechanisms moved.
- **Counter-evidence:** none overturns it. Both candidates fail — `bullet_count` counts FINAL-resume bullets, gated behind the --grounding-signals opt-in, serves as a denominator only, not the suggested-bullet count; `selected_bullets` appears only as hand-counted TUNING_LOG figures, never an automated/charted metric. The eval pipeline never invokes recommend/_dropoffPick, so the AL-1 count is never produced in eval data. PROMPT_VERSION moved ~13 times; commit a5ae53c added _dropoffPick AND bumped PROMPT_VERSION with no bullet-count metric — substantiates "both have moved." One mild overstatement (not verdict-changing): the "small, label-free add" remediation conflates generated-resume bullets (opt-in) with the compose-recommendation count AL-1 is about, so instrumenting the exact signal is somewhat more than trivial reuse. P1 holds (S-3 owner's furthest-below-bar; T-B steered by anecdote).

### F-eval-02 — real loop never exercised; L1/L2 uncalibrated (blocks M-2/T-D) — **CONFIRMED**
- **Tried:** confirmed c6e0437 is an ancestor of HEAD with zero diff on fixtures/real; `git ls-tree` (only .gitkeep); tested the strongest counter-angle (labels could be gitignored — .gitignore:52-53 ignores the dir, so git-absence alone isn't sufficient) BUT the finding rests on affirmative in-repo prose; searched for a contradiction (a TUNING_LOG real-run entry or a PV-1/PV-2 completion marker — none; found corroboration at TUNING_LOG:394-401); tested "blocks M-2"/"L0 untuned" scope; verified exact line numbers; verified the sequencing.
- **Counter-evidence:** none — held on every axis. Nuance: the literal "ls-files shows only .gitkeep" is necessary-but-not-sufficient (dir gitignored), but the load-bearing evidence is the affirmative documentation (GROUNDING_METRIC.md:93-98 "live run was never executed"; hardening.py:591 UNCALIBRATED; TUNING_LOG:394-401), which the project would have corrected had a live run occurred. Charter trace exact (M-2:269-270; T-D:334-336 near-verbatim; C-3). FIX/P1 critical-path severity supported.

### F-eval-03 — no lay metrics legend on groundedness / tuning panes (S-3) — **CONFIRMED**
- **Tried:** re-derived all cited evidence (pin = v1.0.5); confirmed the dashboard route is live (not dead code); verified the bare technical labels + dev-register tuning banner; searched the WHOLE template for an existing lay legend (the strongest refutation); cross-checked S-3/M-2/A-2/E-2 and the disposition/leverage taxonomy; reasoned about framing a v1.0.7 forward criterion as "absent at the pin."
- **Counter-evidence:** none breaks it. The nearest candidate — the detail panels carry explanatory text (:668/:693/:670-673/:697-714) — does not refute: that text is itself dev-register ("L0 is a flag-for-review signal... false-positive on paraphrase. Uncalibrated until labels exist"), exactly the internals needing a lay gloss; sibling F-expa11y-04 concurs. S-3/M-2/A-2/E-2 all valid. "P1" is the 10-Principles Hardening reference, not an inflated priority; the finding scopes the legend as a v1.0.7 forward criterion "absent at the pin," not a present-tense bug.

### F-eval-04 — dynamic source-union scoring distinguishes asserted-beyond from synthesized-within (C-3) — **WEAKENED**
- **Tried:** re-derived all cited evidence; read assemble_source_union, the widened GROUNDING CHECK + worked examples, corpus-mode by-construction, GROUNDING_METRIC.md:24-31; grepped both production callers; **dynamically ran** the extracted function with edited_resume_text populated to test whether typed edits fold into the metric union; traced save_iteration_context to see whether typed edits ever land in resume.text; checked whether generate() calls assemble_source_union.
- **Counter-evidence:** the finding (echoing GROUNDING_METRIC.md) states the METRIC scores against a four-part union including "first-person typed edits." Code folds only THREE: assemble_source_union reads resume.text + supplementals + clarifications only. Dynamic run with edited_resume_text='TYPED_EDIT_FACT' returned len-3, typed edit NOT folded; test_hardening confirms three sources; save_iteration_context POPS edited_resume_text and never writes it to resume.text. Typed edits reach grounding only via the prompt (accepted) and as the scored draft text — the subject scored, not a source block. Everything else CONFIRMED (shared by the two deterministic callers; dynamic union not original-only; prompt widens with worked examples; corpus mode grounded by construction). Citation nit: doc is at docs/dev/.
- **Revised:** keep as **P1 AFFIRM** for C-3/R2-4.4 — severity unchanged. Narrow the sub-claim: the deterministic METRIC's source union folds **THREE** sources (primary + supplementals + clarification answers), NOT typed edits. Typed edits are accepted prompt-side and present as the scored draft text, but are not a source-union element. `GROUNDING_METRIC.md:24-31` overstates this (four parts) — flag as a doc/code drift to fix alongside the affirmation so the AFFIRM doesn't lock in an inaccurate four-part metric-union claim.

---

## Open-source readiness, security & privacy

### F-sec-01 — C-2 egress asserted in prose, not by a committed falsifiability test — **CONFIRMED**
- **Tried:** whole-tree search for an egress/allowlist/socket-blocking test + pytest-socket dep (none); read ci.yml (no egress job); tried to show it's already scheduled (grepped RELEASE_ARC/CHECKLIST/CHANGELOG/SECURITY for egress/falsifiability — zero, not on any roadmap); tried to show the C-2 claim is false so a test would be moot (enumerated the outbound surface = scraper + Anthropic SDK -> enumerable, feasible); checked the scraper has no internal host allowlist; verified the cited lines.
- **Counter-evidence:** none — the severity probe strengthened it. At c6e0437 dashboard.html:15 loads Chart.js from a CDN at runtime — a live destination the charter rules a confirmed C-2 violation — yet C-2's prose asserts it was "verified at c6e0437." A committed egress falsifiability test would have flagged this and keeps the promise after vendoring. E-2 names the badge; C-0 requires by-construction enforcement where a deterministic test can; S-1 ranks PII leak #1. Only nuance: other tests mock the Anthropic SDK at function level (not socket level) — a wording caveat, substance intact.

### F-sec-02 — C-1 loopback bind implicit (Flask default), neither pinned nor asserted — **CONFIRMED**
- **Tried:** re-derived app.py:6988 `app.run(debug=..., port=5000)` (no host=); inspected Flask 3.1.3 Flask.run to confirm the implicit-default mechanism; hunted refutations — host=/SERVER_NAME/FLASK_RUN_HOST/--host anywhere (none); a test pinning the production bind host (none); a network-egress test covering the bind (none yet); whether the console-script entry bypasses the unpinned run (no); whether docs constitute an enforcing pin (prose only); dynamically read Flask.run's host-resolution block.
- **Counter-evidence:** none — corroborating. Flask 3.1.3 resolves host=None to SERVER_NAME else literal "127.0.0.1" (the finding's mechanism). The two per-request guards (_is_localhost_request, dashboard _localhost_guard) are real but different (request.host header checks, not socket binds). SECURITY.md:142 asserts the property in prose but enforces nothing; C-1 carries the categorical "binds to 127.0.0.1 only" with no by-construction test. One omission that strengthens: SERVER_NAME config is a third silent flip vector, unset today.

### F-sec-03 — SECURITY.md "No external CDN is loaded at runtime" false at the pin — **CONFIRMED**
- **Tried:** five break angles — (1) SHA check (`git diff c6e0437 HEAD` on the cited files = empty); (2) re-derived the unconditional `<script src=cdn.jsdelivr.net...>` outside any Jinja guard; (3) vendoring check (`git ls-tree static/vendor/` = only paged.polyfill.js; no chart.umd anywhere); (4) whether SRI/crossorigin neutralizes (pins bytes, browser still fetches); (5) a localhost-only carve-out (none — claim is categorical).
- **Counter-evidence:** none — everything corroborates. SECURITY.md:85 + the no-CSP accepted-risk row (:144 "no third-party content is loaded") are both contradicted by dashboard.html:15. Independent corroboration: test_dashboard_routes.py:378 asserts `"chart.umd" in body` — a committed test ENFORCES the CDN tag, so PX-01 is provably not landed. Charter rules this at C-2(i); bears on C-0/C-2/P-3/S-1.

### F-sec-04 — egress enumeration divergent (SECURITY.md 3 classes incl. phantom JD-URL fetch) — **CONFIRMED**
- **Tried:** confirmed the pin + byte-identical cited files; re-printed exact lines; tried to find a real JD-URL fetch (grepped jd_url/job_url/fetch; traced every jd_url usage); tried to find a path where a pasted JD URL gets scraped; tried to dismiss the HF/D-6 recommendation as overreach (grepped SECURITY.md for huggingface/transformers/grounding/download); verified the governance "ruled v1.0.6 / not landed" claims.
- **Counter-evidence:** none — everything corroborates. jd_url is provenance on the Application row, never passed to fetch_url_content; the scraper reads only linkedin/website/portfolio URLs; the sole outbound is scraper.py:46. JD content arrives as the 'job_description' textarea, not a URL. SECURITY.md = 3 classes (a/b/c), README = 2, vision = 2. Charter C-2(iv)/PX-03 rules the docs fix; the HF recommendation is grounded (SECURITY.md has zero HF mentions; the auto-download is real ~3.2GB; C-2(ii) rules it D-6-governed/opt-in; the wiki already files HF under egress class (b)). Not landed at c6e0437.

### F-sec-05 — route containment dense, unit-tested, build-time-guarded — **CONFIRMED**
- **Tried:** re-derived every count (_safe_username=82, _within=59, secure_filename=25, routes=78); confirmed def-lines + 9 security tests pass (6.21s); verified block-secrets blocks the key shape + key-file shapes; to BREAK "done right," AST-enumerated all 78 routes for filesystem access lacking the guards in-body, inspected each flag/<path:> route/send_file site, sandbox-reasoned Flask converter traversal semantics.
- **Counter-evidence:** partial, non-refuting. Containment is NOT uniformly routed through _within: download_file inlines its own containment check; _load_config/_save_config build a path from a RAW username with no secure_filename; get_config/update_config/list_resumes feed unsanitized usernames. So the density figure is helper-USAGE count, not proof every FS route calls the helpers. BUT exploitability is low: Flask's default string converter does not match '/', so single-segment inputs stay inside the parent dir; the one <path:> route is containment-guarded inline; all three send_file sites are guarded. Gaps are defense-in-depth, not open traversal holes — the safety/charter claims hold.
- **Revised:** keep CONFIRMED/KEEP/P1. One honest narrowing (no verdict/severity change): read the 82x/59x as helper-usage density, not a guarantee that every FS route routes through the audited helpers; the 9 tests cover the helpers in isolation, not route-level enforcement. These low-exploitability gaps reinforce the finding's own WATCH rider — a blueprint split should add route/integration traversal tests, close the secure_filename gap in _load_config/_save_config, and re-scope the hook beyond app.py.

### F-sec-06 — fresh hostile clone carries zero real PII and zero secrets — **CONFIRMED**
- **Tried:** scoped every check to c6e0437; `git diff c6e0437 HEAD -- .gitignore` empty; inspected both allow-listed configs + both Casey Rivera fixtures; scanned ALL tracked blobs for the key shape (zero); broad secret-shape content scan (zero); secret+db filename scan (only the 2 synthetic .config files); `git check-ignore -v` on real-PII paths (each caught by a specific line); tree-wide email scan (only synthetic/vendor); sandboxed gitignore directionality test; STRONGER lens — full-history secret scan of 2638 objects + git grep across all commits (both zero), because a hostile clone carries history.
- **Counter-evidence:** core holds even under the history lens the finding only partially tested. Fixtures are unambiguously synthetic (RFC-2606 domains, 555-01xx phones, fictional companies, self-documented). The three non-example.com email-like strings are all non-PII (code-comment example, test literal, git SSH URL). One minor wording imprecision (not a defect): the affirm rationale mis-identifies the load-bearing lines — removing the `!`-prefixed negations only loses synthetic fixtures; the actual leak-prevention is the broad `configs/*.config`/`resumes/*`/`db/*.sqlite` ignores.
- **Revised:** core claim and P1/KEEP affirmation stand. Optional rationale tightening: the protective value is in the broad ignore lines; the `!` allow-list negations protect the synthetic fixtures, not the user. Frame the affirm as "so a future .gitignore refactor doesn't weaken the broad PII ignores or accidentally track a real config."

### F-sec-07 — two human-response SLAs survive at the pin (D-4 softening pending) — **CONFIRMED**
- **Tried:** re-derived both SLA strings at c6e0437 with exact line numbers; under-count check (grepped all md + non-md for business-day/respond-within/30-days/SLA, excluding review artifacts -> exactly two source docs); already-softened check (the only best-effort hits concern scraping); already-prescribed/scheduled check (no D-4 SLA-softening prescription in RELEASE_ARC/CHECKLIST/CHANGELOG); public-framing check (both are root community-health docs); charter trace (D-4 verbatim names the SECURITY.md promise); COC origin via git log.
- **Counter-evidence:** none — held on every axis. One texture nuance: SECURITY.md "We aim to respond" (partial softener) vs COC "will respond" (firmer), but D-4 targets the day-count SLAs regardless and names this very promise. Severity anchor solid (D-4 + P-3).

### F-sec-11 — CODE_OF_CONDUCT routes vuln/conduct reports to the WRONG repo — **CONFIRMED**
- **Tried:** re-derived COC:13 (Cooksey/resume advisories) and all amodal1/callback refs at the pin; tested whether Cooksey/resume is canonical (pyproject name/author/urls all amodal1/callback -> stale provenance); tested redundancy (grepped the findings dir for Cooksey/wrong-repo/disclosure-channel — zero; F-sec-07 cites COC:15 not :13; F-sec-09 never flags the wrong repo -> genuinely additive); tested severity vs P-3 and the FIX security tier; checked git remotes + SECURITY.md's own channel (repo-relative/correct, unlike COC).
- **Counter-evidence:** none weakening — found scope-broadening counter-evidence that strengthens: the same broken URL also appears at .github/ISSUE_TEMPLATE/config.yml:4 (New-Issue contact-links banner, arguably more prominent) and :7 routes Discussions to Cooksey/resume; stale Cooksey/resume provenance also persists in .claude-plugin/plugin.json:8 and evals/schemas/context_set.schema.json:3. P1/FIX consistent. Trace holds (E-2 PVR precondition, P-3, S-1). One non-load-bearing caveat: could not network-verify the 404 (sandbox), but the dispositive fact is it's not the canonical repo.

---

## Documentation & wiki architecture

### F-docs-01 — SECURITY.md asserts a JD-URL egress class the code lacks; 3 docs disagree — **CONFIRMED**
- **Tried:** re-derived all three doc locations from the git object at the pin (not the worktree); searched for any code path fetching a pasted JD URL (grepped jd_url/job_url; read scraper, the analyze routes, db/build_context); grepped all callers of fetch_url_content/fetch_profile_content + *.js for a frontend JD-URL fetch; read the signed charter C-2(iv)/PX-03 to test severity; verified the AL-7 provenance.
- **Counter-evidence:** none weakens it. SECURITY.md:57-59 states an HTTP-call class "(c) any URL you paste as a job description"; vision/README each enumerate exactly two. jd_url is stored only as provenance (db/build_context.py:125), never passed to fetch_url_content; the scraper's only inputs are linkedin/website/portfolio; no JD-URL fetch in *.js. PX-03 unlanded at the pin. The finding correctly claims a destination-claim-with-no-code, NOT an actual leak. One headline-shorthand nuance ("an egress class the code does not have" — the field exists as metadata but is never fetched); the body states the precise mechanism.

### F-docs-02 — SECURITY.md:85 "No external CDN is loaded at runtime" false at the pin — **CONFIRMED**
- **Tried:** re-derived at the exact pin via `git show` (c6e0437 ancestor of HEAD, code/doc lines unchanged); five break angles — (1) is the Chart.js `<script>` conditionally suppressed (NO — unconditional, no Jinja guard, render returns the template directly; test_dashboard_routes.py:378 asserts chart.umd in body even in empty-state); (2) did PX-01 vendoring land (ls-tree static/vendor/ = only paged.polyfill.js -> UNLANDED); (3) a second false-premise location (SECURITY.md:144 "no third-party content is loaded"); (4) P1 severity vs charter; (5) the pin SHA itself.
- **Counter-evidence:** none — every angle corroborated. SECURITY.md:85 verbatim; dashboard.html:15 loads cdn.jsdelivr.net Chart.js unconditionally; static/vendor/ has no chart.js; SECURITY.md:144 repeats the false premise. Charter C-2(i) independently rules a confirmed no-CDN violation; C-0 cardinal-sin class; S-1 fear #2; sibling F-sec-03 independently rates P1. The finding scopes itself to the DOC overclaim.

### F-docs-03 — "The LLM cannot invent facts" — flat C-0-barred absolute owner flagged overstated — **CONFIRMED**
- **Tried:** re-derived all four cited quotes via `git show`; checked line-number precision; tested whether C-0's deterministic-test exception could save the absolute; verified "owner twice called this overstated" against R2-4.2/R2-4.4; scrutinized whether overview.md:87 "open revision point" supports the overclaim claim; searched for any place the docs already soften the lead.
- **Counter-evidence:** core holds. vision.md:50 = "The LLM cannot invent facts." (exact). Owner words verified (R2-4.2/R2-4.4 titles confirmed, so "twice called overstated" is accurate). C-0 signed; the grounding_overlap metric only MEASURES (does not enforce by construction), so the C-0 exception does not apply. One genuine overstatement: the finding says "overview.md:87 already lists this as an open revision point," but revision point #4 is an editorial ORDERING question that keeps the phrase verbatim — it does NOT flag the absolute as overstated; the owner's flag lives in R2-4.2/R2-4.4. Citation drift: "may not fabricate" is overview.md:26 (not :27); "without inventing anything about you" is llms.txt:4 (not :6).
- **Revised:** FIX/P1 stands (docs-register violation of signed C-0 across four surfaces, traceable to R2-4.2/R2-4.4). Two narrowings: (1) drop/rephrase the ":87 already lists this" sentence — that revision point is about lead-ordering, not the overclaim; the actual acknowledgment is R2-4.2/R2-4.4. (2) Fix line anchors: "may not fabricate" = overview.md:26; "without inventing anything about you" = llms.txt:4. Quotes accurate; only anchors off by 1-2.

### F-docs-04 — public docs describe a live profile/website scrape that is dead code at the pin — **CONFIRMED**
- **Tried:** tried to break "no runtime caller" every way at c6e0437 — full-tree grep for fetch_profile_content/fetch_url_content + `import scraper`; AST scan of app/analyzer/hardening/build_context/corpus_import for any scraper import/call; chased the 559bd62 commit-message claim ("used by the importer + dashboard") by grepping onboarding/ and dashboard/; traced profile_text back to its real source; `git log -S` to test regression-vs-never-wired; checked whether docs caveat the scrape and whether a UI affordance supplies a URL.
- **Counter-evidence:** core holds robustly. The ONLY references are scraper.py (def + self-call), test_scraper (tests fetch_url_content only), CHANGELOG prose. AST: zero scraper imports/calls in runtime modules. The caller was removed in 559bd62; the DB path sources profile_text from Candidate.profile_text, never re-wiring the fetch. The commit message's "used by the importer + dashboard" is FALSE at the pin (strengthens the finding). UI has live URL inputs + persistence, mirroring the live-scrape expectation. TWO corrections: (1) the non-dependency-downloads.md:31 citation is mis-applied (that page enumerates downloads, not egress; the scrape isn't mentioned there) — strike it; (2) severity mildly high as a clean FIX/P1 — the charter already rules this (C-2(iii) PX-02 + C-2(iv) PX-03, v1.0.6), so it's confirmatory verification, and the harm is a graceful no-op (less egress than documented), second-tier S-1 not top-tier.
- **Revised:** keep CONFIRMED at FIX, narrow two things. (a) Evidence: strike non-dependency-downloads.md:31; the load-bearing citations are SECURITY.md:57-59, vision.md:89-90, README.md:127, plus reinforcing code at static/app.js:230 and the dead `scrape_url()` API name in docs/architecture.md:197. (b) Severity/scope: frame as confirmatory verification that PX-02 (re-wire) + PX-03 (docs-correct) are UNLANDED at c6e0437, in the second-tier S-1 band (amateurish-execution appearance / P-3 / C-0), not the top-tier PII-leak band — runtime effect is a graceful no-op; the fix is already prescribed for v1.0.6.

### F-docs-05 — install docs fold Chromium ~150 MB into base prerequisite — **WEAKENED**
- **Tried:** re-derived every cited line (HEAD b85be08; extracted install.md at c6e0437 -> byte-identical); verified the Chromium prerequisite bullet + the three mandatory numbered OS steps; attacked the technical premise (generator.py:102-106 routes only the .pdf branch through Playwright; the in-browser preview uses render_html_string "without invoking Chromium"; app.py has zero playwright imports) -> the <5min DOCX smoke user genuinely needs no Chromium, the kernel holds; then attacked the D-6 "contradiction" and P1 severity by reading D-6 verbatim and the wiki page the finding cites.
- **Counter-evidence:** the "contradicts D-6" framing is overstated and one-sided. (1) D-6 literally reads "dev work (Chromium)" — shorthand — but PDF is a FIRST-CLASS end-user output format (install.md:220, README.md:110; README.md:37 tags the step "for PDF output"). (2) The wiki page the finding ITSELF cites (non-dependency-downloads.md:22-27) classifies Chromium under "(a) To run the basic tool" / "the single biggest non-pip download for normal use" — directly contradicting the "dev-only" premise. So there is genuine charter-internal tension, but the finding presents only one side. (3) P1 too high: install.md labels the step "for PDF rendering" at every occurrence, so no user is misled — worst case a DOCX-only user runs one unneeded ~150MB download (touches no S-1 fear). (4) The wiki also wrongly claims Chromium renders the live preview (it's Chromium-free), so the doc landscape is itself inconsistent.
- **Revised:** keep the factual observation, drop the overstatement, downgrade to **~P3** (docs nit, FIX). Accurate narrowed claim: install.md places the ~150MB step in base Prerequisites + as a mandatory numbered step on all three OS paths, even though a fresh-clone skip-clarify DOCX-output user needs Chromium only for PDF (DOCX and the live preview are both Chromium-free). Optional polish: gate the step behind an "if you want PDF output" conditional. This is NOT a clean D-6 contradiction — the docs around D-6 are mutually inconsistent (D-6 shorthand vs wiki/README basic-tool classification), so the real action is reconciling that classification, not asserting install.md violates the charter.

---

## Governance, memory & incubation

### F-gov-01 — block-merge hook misses the dominant direction; convention-only for the common path — **CONFIRMED**
- **Tried:** ran the exact hook regex in a sandbox (with obfuscated trigger tokens — the live hook blocked the un-obfuscated attempt, itself a meta-confirmation it fires) across 9 command variants; four angles — (1) re-derive the regex result; (2) is the unblocked feature-merge form the documented dominant direction or a strawman; (3) does the system-model "Regulation = machine-enforced" classification exist; (4) any defense-in-depth (a second Bash-merge guard or process layer) that moots it.
- **Counter-evidence:** none. Regex re-derivation matches: the routine feature-merge form PASSES (the hook's own help-text example also passes); only the reverse direction / push blocks. The passing form IS the documented standard (CONTRIBUTING:52, git-flow.md:19, the close-out checkout-then-merge step). system-model.md:97-101 files the hook machine-enforced -> the gap makes it vigilance-only for the common path = the C-0 seam. No second guard (require-feature-branch gates only Edit|Write; cleanup hooks are PostToolUse state-managers). Severity calibrated (governance-internal; two sound witness-class fixes incl. detect HEAD==main via `git rev-parse --abbrev-ref HEAD`, which F-gov-02 confirms is worktree-local).

### F-gov-02 — two live W-1 collisions structural in code; no isolation rule as governance — **CONFIRMED**
- **Tried:** re-derived every cited line (worktree == c6e0437 for these hooks); read all four plan hooks; (1) sandbox worktree test (`git rev-parse --abbrev-ref HEAD` is worktree-LOCAL -> validates the finding's own correction that require-feature-branch:36 is not worktree-blind); (2) drove cleanup-plan-on-merge with crafted JSON (only wipes on a Bash success-string merge — testing whether "on any session's merge" overstates); (3) grepped the whole repo for a written isolation rule.
- **Counter-evidence:** structural facts hold. check-plan-approved uses a single global ~/.claude/plans/ + .approved marker, invalidated when any newer *.md appears (a second session trips it); cleanup deletes ALL *.md + the marker; no per-session/worktree scope in any plan hook. The isolation rule appears ONLY in review artifacts, not production governance (docs/governance/ doesn't exist yet; CONTRIBUTING:219 "Future: multi-agent identity" is auth, not isolation). One minor looseness: the one-line "Claim" omits the PowerShell/`--no-ff`-only caveat, but the full source finding records that mitigant (lowers frequency, not the hazard) — not overstated overall.

### F-gov-03 — serial-session framing still authoritative; real parallel model uncodified — **CONFIRMED**
- **Tried:** re-derived every cited line at the pin; six refutation angles — (1) fabricated/wrong line numbers; (2) is codification already scheduled in RELEASE_ARC Phase 4.7 (read all of it); (3) repo-wide grep (excl. review/) for worktree/concurrent/isolation/branch-ownership under any terminology; (4) is the CONTRIBUTING:219 cite overclaimed re isolation; (5) verified "parallel worktree contradicts docs daily" via `git worktree list`; (6) tested severity by verifying the two mechanical collisions in hook source + P1/FIX against the rubric. Read R2-11.
- **Counter-evidence:** none breaks it. RELEASE_ARC:390/863 ("one branch per session") match exactly; CONTRIBUTING:219 is identity/auth not isolation; AGENTS.md close-out is single-closing-agent voice; R2-11 explicitly directs naming isolation rules + retiring the "Future:" framing. The one nuance is NOT counter-evidence: a Governance-extraction epic IS scheduled v1.0.7 but only RELOCATES existing rules — it does not author the parallel/isolation governance (grep for multi-agent/parallel/worktree/R2-11 returned zero), so the serial framing carries forward. `git worktree list` shows two live worktrees -> "contradicts daily" is literally true. The finding self-discloses the CONTRIBUTING:219 caveat. P1/FIX matches the rubric.

### F-gov-04 — seven enforced blocker hooks real and honestly separated from witness/tribal rules — **CONFIRMED**
- **Tried:** confirmed the hook/settings state is byte-identical to the pin (`git log c6e0437..HEAD` zero changes); counted `exit 2` across all 10 hooks; read full bodies of 7 to confirm exit-2 lines are reachable blocking statements (each emits a BLOCKED stderr); confirmed settings.json = 96 lines, exactly 7 distinct blocker scripts under PreToolUse + 3 witnesses under PostToolUse; grepped hooks for PROMPT_VERSION/pyproject|CHANGELOG/close-out|sweep|handoff to test whether any "tribal" rule secretly has a hook. Tried hardest to break on: an exit-2 in a witness (none), an unreachable exit 2 (none), a miscount of 7 (exact), a hook enforcing the four prose rules (none).
- **Counter-evidence:** core claims hold; no substantive counter-evidence. Minor citation-locus imprecisions: (1) "AGENTS.md:41,96" — :41 is the PROMPT_VERSION prose (correct), but :96 is the pipeline-diagrams pointer; the new-dep prose lives in CONTRIBUTING (D-1 footnote) + the AGENTS "What NOT to do" list. (2) Two blockers carry documented env-var escape hatches and validate-context skips if jsonschema absent — conditional, but each still reaches exit 2 on the block path, so "enforced blocker" is accurate. exit2 counts: check-plan-approved=2, require-feature-branch=1, block-secrets=3, validate-context=2, route-security-lint=1, block-merge-to-main=1, ruff-changed=1; witnesses=0.

### F-gov-05 — governance-extraction design register-grade (extract-don't-restate, one home, @import safety) — **CONFIRMED**
- **Tried:** re-derived every cited atom (governance-extraction.md byte-identical c6e0437..HEAD); checked the page states extract-don't-restate overriding register-in-place, mixed-doc-keeps-pointer, the harness-auto-loaded @import hard constraint verbatim, grounding-rule compliance ([synthesis]/[[backlinks]]/reference-not-restate vs SCHEMA.md D5), the 3 open sub-decisions + no docs/governance/charter.md at the pin; cross-checked RELEASE_ARC; tested the @import mechanism is real (CLAUDE.md:16 literally `@AGENTS.md`); probed C-0 compliance and the settled-vs-open tension.
- **Counter-evidence:** one real flaw found, but NOT in the finding under review: governance-extraction.md carries a STALE cross-ref ("RELEASE_ARC Phase 4.5" at :8/:73, but the arc moved governance extraction to Phase 4.7 on 2026-06-12). The full assessment-source version repeats the stale 4.5. However, the condensed finding does NOT inherit it (says "tracked to v1.0.7" = the accurate destination, Phase 4.7). So the verified claim is correct. No counter-evidence undermines any substantive claim; P1 apt (W-2 graduation vehicle, C-0 honored).
