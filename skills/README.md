# skills/

Scaffold only — no skill content yet. Mirrors the `commands/`/`agents/`/`hooks/`
root-level convention (kit-adoption commitment 3, `KIT-5`).

**Open follow-on** (explicitly out of scope for `chore/hook-dispatcher`):
importing the `context-structure-review` skill from the external
agent-coding-practices kit (see [`docs/dev/kit-adoption-design.md`](../docs/dev/kit-adoption-design.md)
§3 Decision 5, §7 Phase 5). The kit's source path isn't recorded in
`CLAUDE.local.md` yet — needs a `CLAUDE.local.md` entry (or equivalent) pointing
at the kit checkout before that import can happen, then its own branch.
